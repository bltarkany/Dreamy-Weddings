const { Event, Guest, EventGuest } = require('../models');

module.exports = {
  // TODO - get layouts
  async namehere({ user }, res) {
    try {
    } catch (error) {
      res.status(500).json();
    }
  },
  // TODO - get by page
  // TODO - create layout
  // TODO - update layout
  // TODO - delete layout
};
